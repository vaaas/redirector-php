{
  "require-dev": {
    "phpstan/phpstan": "^2.1"
  },
  "scripts": {
    "dev": [
      "Composer\\Config::disableProcessTimeout",
      "php -S 0.0.0.0:8000 index.php"
    ],
    "check": "vendor/bin/phpstan analyse -l 10 index.php app"
  }
}
